name: run

on:
  workflow_dispatch:
  push:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: clone
        uses: actions/checkout@v3
      - name: step00
        run: (cd step00_hw; ./run.sh)
      - name: step01
        run: (cd step01_cmdline; ./run.sh)
